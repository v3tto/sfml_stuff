#ifndef RESURRECT64_HPP
#define RESURRECT64_HPP

#include <SFML/Graphics.hpp>

namespace MyColors
{
    const sf::Color DeepPlum(46, 34, 47);
    const sf::Color DustyEggplant(62, 53, 70);
    const sf::Color MutedLavender(98, 85, 101);
}

#endif
